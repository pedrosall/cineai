# requirements.txt del FRONTEND (Streamlit).
# Deliberadamente mínimo: solo lo que app/streamlit_app.py importa.
# El backend (FastAPI + modelo) tiene su propio requirements.txt en backend/,
# con sus propias versiones — son dos servicios independientes.
streamlit==1.57.0
requests==2.32.3